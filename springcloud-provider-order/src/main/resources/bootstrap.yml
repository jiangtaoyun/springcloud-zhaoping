
spring:
  application:
    name: springcloud-provider-order

  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: springcloud-config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
